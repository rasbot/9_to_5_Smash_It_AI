using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class FlameDamage : MonoBehaviour {

    //public float flamerDamage;
    //public GameObject bot;

    //void OnTriggerStay(Collider other)
    //{
    //    if (other.CompareTag("MainCamera"))
    //    {
    //        PlayerController.s.AddHealth(-flamerDamage * Time.deltaTime);
    //        StartCoroutine(PlayerController.s.PlayerHitFlashDelay(0.2f));
    //        print("flamer damage! player health is = " + PlayerController.s.currentHealth);
    //    }
    //}
}
